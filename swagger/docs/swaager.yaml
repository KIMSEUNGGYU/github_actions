# /docs/swagger.yaml
paths:
  /:
    get:
      tags:
        - api/v1
      summary: get index page infos
      security:
        - ApiKeyAuth: []
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/200"

definitions:
  200:
    type: object
    properties:
      code:
        type: integer
        format: int3
        example: 200
      status:
        type: string
        example: "success"
      message:
        type: string
        example: "swagger test 1"
      result:
        type: object
        additionalProperties: {}
